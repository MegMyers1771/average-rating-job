# Генератор отчётов по рейтингам ов

Простая утилита на Python для чтения данных из CSV-файлов, вычисления среднего рейтинга по брендам и вывода форматированных отчётов в консоль.
Поддерживается работа с несколькими файлами и автоматическое форматирование таблиц.

---

## 🧾 Пример CSV-файла (`examples/laptops.csv`)

```csv
name,brand,price,rating
ThinkPad T42,IBM,1850,4.5
PowerBook G4,Apple,2400,4.6
VAIO VGN-TX,SONY,2100,4.3
Latitude D610,Dell,1450,4.1
Pavilion zd8000,HP,1300,3.9
...

```

---

## ▶Использование

Запуск из командной строки:

```cmd
py main.py --files ram.csv --report "Hardware report"
```

Можно указать несколько файлов:

```bash
python main.py --files cpu.csv ram.csv gpu.csv --report "Hardware compare"
```

### Аргументы командной строки

| Аргумент | Описание | Значение по умолчанию |
|-----------|-----------|----------------------|
| `--files` | Пути к одному или нескольким CSV-файлам | `!Required` |
| `--report` | Название отчёта, отображаемое в консоли | `"Main report"` |

---

## 🖥️ Пример вывода

```
+-- Report for examples/gpu.csv --+
+---+---------+--------+
|   | brand   | rating |
+---+---------+--------+
| 0 |  NVIDIA | 4.5    |
+---+---------+--------+
| 1 |  AMD    | 4.4    |
+---+---------+--------+
| 2 |  Intel  | 3.9    |
+---+---------+--------+
```




### Запуск тестов:
```cmd
pytest test_parser.py -v
```
или
```cmd
pytest test_report_generator.py -v
```

или общий
```cmd
pytest  -v
```

Установка зависимостей:
```cmd
pip3 install -r requirements.txt
```
